syntax = "proto3";
// The request message containing the user's name.
service piml {
  // Sends a greeting
  rpc get_residual (Array) returns (Array) {}
} 
message Array {
  int32 n_cols = 1;
  int32 n_rows = 2;
  repeated double val = 3;
}
